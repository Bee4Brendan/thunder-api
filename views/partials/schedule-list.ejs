<ul class="schedule-list">
    <% thunderSchedule.forEach(game => { %>
        <li 
            class="schedule-item liquid-glass
                <%= game.hasBeenPlayed ? 'past-game' : '' %> 
                <%= game.isThunderHomeGame ? 'home-game' : 'away-game' %>"
            data-home="<%= game.isThunderHomeGame %>"
            data-away="<%= !game.isThunderHomeGame %>"
            data-next="<%= game.isNextGame %>"
            data-starred="<%= game.isStarred %>"
        >
            
            <!-- <div class="game-number">
                <%= game.time %>
            </div> -->

            <div class="game-info">
                <div class="game-date">
                    <%= game.longDate ? game.longDate + " - " : "" %>
                    <span><%= game.time ? game.time : "" %></span>
                </div>

                <div class="matchup">
                    <span><%= game.home_team.name %></span>
                    <strong>vs</strong>
                    <span><%= game.visitor_team.name %></span>
                </div>
            </div>
            <img id="opponent-image" src="/images/team-logos/<%= game.opponent %>.svg" />

            <!-- <% if (game.isStarred) { %>
                <div class="star">⭐</div>
            <% } %> -->

        </li>
    <% }); %>
</ul>